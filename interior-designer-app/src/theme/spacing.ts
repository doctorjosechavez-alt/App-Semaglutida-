// Escala de espaciado. Los botones/campos usan tamaños grandes a propósito
// (uso con una mano, en obra, a veces con guantes o el teléfono sucio).
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;

export const radius = {
  sm: 8,
  md: 14,
  lg: 20,
} as const;

export const touchTarget = {
  minHeight: 56,
} as const;
